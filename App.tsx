import React, {ReactNode} from 'react';
import {StatusBar} from 'react-native';

const App: () => ReactNode = () => {
  return (
    <>
      <StatusBar barStyle="dark-content" />
    </>
  );
};

export default App;
